# Antislop Audit 006

Target: Card Library at `/`

1. **HIGH** `components/card-library.tsx:68-116` - Header keeps the desktop title and 204px language/account cluster in one row without a narrow-screen reflow, risking horizontal overflow below common 360px viewports. Violates R-03.
2. **MEDIUM** `components/card-grid-item.tsx:7-36` - A card without an allowed local image shows only its type, so multiple real cards can become visually indistinguishable even though their name and code are available. Violates C-2 and C-4.
3. **LOW** `app/(dashboard)/loading.tsx:10-27` - Loading state omits the language/account controls, Filter control, and count divider, causing avoidable layout shift when content resolves. Violates R-27 and C-4.

## Mechanical Check

`impeccable detect --json` returned no findings for the changed UI files.

## Verification Limit

The application returned HTTP 200 with the expected database-backed empty state. No supported browser executable is installed in this environment, so viewport screenshots and pixel comparison against the supplied PNG could not be produced.
